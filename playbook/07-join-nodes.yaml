---
- hosts: k8s-worker
  tasks:
    - name: Query token for join
      ansible.builtin.command: kubeadm token list | awk '{print $1}'
      register: token_list

    - name: Create a token when not found
      block:
        - name: create by kubeadm
          ansible.builtin.command: kubeadm token create

        - name: Query token again
          ansible.builtin.command: kubeadm token list | awk '{print $1}'
          register: token_list
      when: token_list.stdout == ""

    - name: Get target token
      ansible.builtin.set_fact:
        token: "{{ token_list.stdout.split('\n')[1] }}"

    - name: Get hash value
      ansible.builtin.shell:
        cmd: >
          openssl x509 -pubkey -in /etc/kubernetes/pki/ca.crt |
          openssl rsa -pubin -outform der 2>/dev/null |
          openssl dgst -sha256 -hex | sed 's/^.* //'
      register: hash_value

    - name: Join node to k8s cluster
      ansible.builtin.shell:
        cmd: >
          kubeadm join --token {{ token }}
          {{ hostvars[hostvars['node01']['groups']['k8s-master'][0]].ansible_host }}:6443
          --discovery-token-ca-cert-hash sha256:{{ hash_value.stdout }}
        # --control-plane if this node is master node